Commands:

	Group create
	Group leave
	Group list

	View information about group
	List members of group

	Invite player
	Kick player

	Cancel requests

Supported databases:
	* MySQL/hsqldb
	* cqengine with json

	Probably supported:
		* OrientDB

Storage:
	* In database if it supports persistence
	* JSON backups or data source for pre-loading

Text renderer
	* Headings
	* Separators
	* Tables
	* Tree like structure
    * Accepts incoming data which is renderable

Concurrent member and group controlling
