- verify
- fix states
- predefined ranks

- list only verified
- cleanup for json engine
- set home only once/ time delay
- rename only colors
- remove e.printStackTrace()
- pvp stuff ff allow/auto
- BulletinBoard

- Support sponge
- coords sort
- log file
- Performance: Lazy loading of settings/ranks


Bukkit dependencies:
 * Player.getLocation()
 * Player.sendMessage(...);
 * Server.getWorld(...);
 * Server.getScheduler();
 * Server.getConsoleCommandSender();
 * ChatColor
 * JavaPlugin

Config:
   Settings:
        kill-weights.rival
        kill-weights.neutral
        kill-weights.civilian

Text renderer:
	* Tree like structure

a:
	Get player name from external source if player is offline and there is no "offline player data"

Caching
	* Pre-rendered listing output
